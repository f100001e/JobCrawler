# JobCrawler

This program is a webcrawler that parses URLs from free databases for mailing campaigns via SMTP. Functionality also includes locally sourced searches via user-selected URLs which are to be listed in apex domain form in target_companies.txt. Forced IPv4 is an option to avoid IPv6 WAN IP lease expiration.  